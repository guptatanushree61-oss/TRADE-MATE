# Example env file for the Supabase Edge Function `assistant`
# Copy this to .env when running `supabase functions serve` locally

# Your Supabase project URL (without trailing slash)
SUPABASE_URL="https://your-project-id.supabase.co"

# Service role key (server-only - keep secret)
# Set this in the Supabase dashboard for Functions or in your local .env when serving locally
SUPABASE_SERVICE_ROLE_KEY="supabase_service_role_key_goes_here"

# Optional: OpenAI API key (if you want the function to use OpenAI to improve responses)
OPENAI_API_KEY="sk-..."
